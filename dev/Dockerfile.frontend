FROM --platform=linux/amd64 node:22.10.0


ARG NODE_ENV
ARG NODE_VERBOSE
ARG NODE_OPTIONS

ENV NODE_ENV=${NODE_ENV}
ENV NODE_VERBOSE=${NODE_VERBOSE}
ENV NODE_OPTIONS=${NODE_OPTIONS}
ENV NG_CLI_DISABLE_INTERACTIVE=true
ENV npm_config_arch=x64

WORKDIR /srv/ui

COPY ../frontend-v3 .

RUN npm install
